Program test;

Var
    a, b, c : Integer;
    k : Real;
    arr : Array[1..100] Of Integer;

Begin

    a:= 1;
    b := 'f';
    c := a + b;
    k := 300.556;
    Output(c);

    Input(k);

    For a:=1 To 4 Step 2 Do
    Begin
        Output(a + k);
        For a:=1 To 4 Step 2 Do
        Begin
            Output(a + k);
            For a:=1 To 4 Step 2 Do
            Begin
                Output(a + k);

                While (k <= 3.46) Do
                Begin
                    If (k > g) Then
                    Begin
                        k := l + 34;
                        r := 'l';
                    End;
                End;

            End;
        End;
    End;

End.
